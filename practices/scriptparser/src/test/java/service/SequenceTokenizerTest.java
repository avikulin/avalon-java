package service;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import service.grammar.SequenceGrammarParser;

import java.util.List;
import java.util.stream.Collectors;

import static org.junit.jupiter.api.Assertions.*;

class SequenceTokenizerTest {
/*    GrammarAnalizer tokenizer;

    @BeforeEach
    void setup(){
        tokenizer = new SequenceGrammarParser();
    }

    @Test
    void tokenize() {
        List<String> res = tokenizer.tokenize("\"$sum = \", $n1, \"+\", $n2, \"-42 = \", $sum");
        String s = res.stream().collect(Collectors.joining("_"));
        assertTrue(s.equals("\"$sum = \"_$n1_\"+\"_$n2_\"-42 = \"_$sum"));
    }*/
}